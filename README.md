This respository is the allow an AdOps team to engage in an entirely instanced version of GTM. It uses Github as the "site" for a container to sit and subsequently be actioned against (ie: build a tag on it).
The container can then be previewed as if changes made have been done on a live site, allowing the team to test and learn without an outside test environment.
